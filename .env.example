# Copy this file to .env and fill in real values. .env is gitignored -- never commit real secrets.

# LLM used for the "AI thinks / makes a to-do list" step
# Get a key at https://opencode.ai/zen -- gives access to Claude/GPT/Gemini etc via one key
OPENCODE_API_KEY=
OPENCODE_MODEL=glm-5.3-flash

# Socket connection to the Minecraft/Baritone side (get exact values from teammate)
SOCKET_HOST=127.0.0.1
SOCKET_PORT=
